# 第 1 課五行圖譜整合說明

第 1 課保留既有的題庫、測驗順序、分數、提示消耗、答題紀錄與完成事件。新版只取代進入正式測驗前的教學內容層，依序提供課前定位、五行卡、相生與相剋關係圖、導師帶做及重點回顧。

新版元件位於 `src/components/lesson/LessonOneAtlas.tsx`，由 `src/pages/LessonPage.tsx` 在第 1 課步驟 1 至 5 載入。原有的測驗題目仍由既有題庫流程接續，因此新版教學結束後會進入原來的選擇題、是非題和配對題。

## 本機驗證

本機 `/lessons/1` 預覽已顯示新版開場，並保留原有頁首、進度、返回主頁、上一節、繼續及跳到測驗控制。`npm run build` 與 `npm run test:run` 已通過；測驗相關的既有 19 項測試均保持通過。

瀏覽器驗證顯示，第 2 步的五行典籍卡能正確切換焦點。選取「水」後，圖譜會同步顯示方向「北」、季節「冬」、情感「恐」與記憶提示「流動、滋養」。

第 3 步已驗證相生與相剋模式切換。相剋模式會顯示木剋土、土剋水、水剋火、火剋金、金剋木，且焦點關係及圖譜標籤會同步更新。

第 4 步的導師帶做已驗證「水對木 → 相生」會顯示「正確。水能滋養木，所以是水生木。」這段練習只提供形成性回饋，並不進入既有總分計算。

第 5 步的三格回顧完成後，按「繼續」會進入原有第 1 課題庫；預覽已顯示正式題目「木元素代表的季節是？」及原有提示、四個選項和檢查按鈕。這確認新版教學層後的測驗流程未被取代。
